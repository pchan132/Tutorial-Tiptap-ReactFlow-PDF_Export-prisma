# คู่มือการใช้งานฟีเจอร์ PDF Export

## 📋 ภาพรวม

ฟีเจอร์นี้ช่วยให้คุณสามารถส่งออกโน๊ตที่สร้างด้วย Tiptap Editor เป็นไฟล์ PDF ได้ โดยมีวิธีการทำงานดังนี้:

## 🛠️ ไฟล์ที่เกี่ยวข้อง

### 1. ไฟล์หลัก

- `lib/pdf-export.ts` - ฟังก์ชันหลักสำหรับสร้าง PDF
- `components/PDFPreview.tsx` - Component สำหรับแสดงตัวอย่างและส่งออก PDF
- `app/export/[id]/page.tsx` - หน้าสำหรับ export PDF แบบ standalone

### 2. ไฟล์ที่อัปเดต

- `components/TableNote.tsx` - เพิ่มปุ่ม PDF Export และ Delete
- `app/test-note/[id]/page.tsx` - เพิ่ม PDF Preview ในหน้าแก้ไข
- `app/globals.css` - เพิ่ม CSS สำหรับแก้ปัญหา Tailwind CSS 4

## 🚀 วิธีการใช้งาน

### 1. จากหน้ารายการโน๊ต (`/notelist`)

1. คลิกปุ่ม "📄 PDF" ในแถวของโน๊ตที่ต้องการ
2. จะเปิดหน้าต่างใหม่สำหรับ export
3. คลิก "ดูตัวอย่าง PDF" เพื่อดู preview
4. คลิก "ส่งออก PDF" เพื่อดาวน์โหลดไฟล์

### 2. จากหน้าแก้ไขโน๊ต (`/test-note/[id]`)

1. แก้ไขเนื้อหาตามต้องการ
2. ด้านล่างจะมีส่วนของ PDF Export
3. คลิก "ดูตัวอย่าง PDF" เพื่อดู preview
4. คลิก "ส่งออก PDF" เพื่อดาวน์โหลดไฟล์

## 🔧 การทำงานของระบบ

### 1. การแปลง Tiptap JSON เป็น HTML

```typescript
// PDFPreview.tsx - ฟังก์ชัน renderContent()
const renderContent = () => {
  // แปลง Tiptap JSON structure เป็น HTML tags
  // รองรับ: heading, paragraph, bold, italic, lists, blockquote
};
```

### 2. การสร้าง PDF

```typescript
// lib/pdf-export.ts - ฟังก์ชัน exportToPDF()
// 1. ใช้ html2canvas แปลง HTML เป็นรูปภาพ
// 2. ใช้ jsPDF สร้างไฟล์ PDF จากรูปภาพ
// 3. คำนวณขนาดให้พอดีกับกระดาษ A4
// 4. จัดการกรณีเนื้อหายาวกว่า 1 หน้า
```

## 🐛 ปัญหาที่แก้ไขแล้ว

### 1. Tailwind CSS 4 + html2canvas

**ปัญหา:** Error "Attempting to parse an unsupported color function 'lab'"

**สาเหตุ:** Tailwind CSS 4 ใช้ CSS color function `lab()` และ `oklch()` ที่ html2canvas ไม่รองรับ

**วิธีแก้:**

- เพิ่ม `ignoreElements` เพื่อข้าม style sheets
- ใช้ `onclone` เพื่อแทรก CSS ที่เรียบง่าย
- เพิ่ม CSS class `.pdf-export-fix` พร้อมสีที่เรียบง่าย

### 2. TypeScript Error

**ปัญหา:** Property 'rel' does not exist on type 'Element'

**วิธีแก้:** ใช้ type assertion `(element as HTMLLinkElement).rel`

## 🎨 การปรับแต่ง

### 1. ปรับขนาดและความคมชัด

```typescript
// lib/pdf-export.ts
const canvas = await html2canvas(element, {
  scale: 2, // เพิ่มความคมชัด (1-3)
  // ...options
});
```

### 2. เพิ่ม Header/Footer

```typescript
// สามารถเพิ่มในฟังก์ชัน exportToPDF
const addHeader = (pdf: jsPDF) => {
  pdf.setFontSize(10);
  pdf.text("My Notes App", 105, 10, { align: "center" });
};
```

### 3. รองรับภาษาไทย

ต้องเพิ่ม Thai Font และตั้งค่าใน jsPDF:

```typescript
pdf.addFont("/fonts/sarabun-regular.ttf", "Sarabun", "normal");
pdf.setFont("Sarabun");
```

## 📝 ข้อจำกัด

1. **ภาษาไทย:** ต้องเพิ่ม font ภาษาไทยสำหรับ jsPDF
2. **รูปภาพ:** ยังไม่รองรับรูปภาพในเนื้อหา
3. **ตาราง:** ต้องเพิ่มการแปลง Tiptap table เป็น HTML
4. **Performance:** ถ้าเนื้อหาใหญ่มากอาจช้า

## 🔄 การพัฒนาต่อ

### Features ที่สามารถเพิ่มได้:

- [x] ปรับแต่ง page margins (20mm รอบด้าน)
- [ ] รองรับภาษาไทย (Thai Font)
- [ ] รองรับรูปภาพ
- [ ] รองรับตาราง
- [ ] เพิ่ม watermark
- [ ] ปรับแต่ง header/footer แบบกำหนดเอง
- [ ] Server-side PDF generation
- [ ] Batch export (export หลายโน๊ตพร้อมกัน)

## 🎨 การปรับแต่ง Margins

ถ้าต้องการเปลี่ยน margins ให้แก้ไขใน `lib/pdf-export.ts`:

```typescript
// ปรับ margins ตรงนี้
const margin = 15; // เปลี่ยนเป็น 10, 20, 25mm ตามต้องการ
const usableWidth = pageWidth - margin * 2;
const usableHeight = pageHeight - margin * 2;
```

และใน `components/PDFPreview.tsx`:

```typescript
style={{
  margin: "15mm", // ให้ตรงกับ PDF margins
  padding: "15mm"
}}
```

## 🧪 การทดสอบ

1. สร้างโน๊ตใหม่พร้อมเนื้อหาหลากหลาย
2. ทดสอบ export PDF จากหน้ารายการ
3. ทดสอบ export PDF จากหน้าแก้ไข
4. ตรวจสอบความถูกต้องของ PDF output
5. ทดสอบกับเนื้อหายาวๆ (มากกว่า 1 หน้า)

## 💡 Tips

1. **Preview ก่อน Export:** ให้ผู้ใช้ดูตัวอย่างก่อนส่งออกเสมอ
2. **Loading State:** แสดงสถานะกำลังทำงานเมื่อ export
3. **Error Handling:** แสดงข้อความ error ที่เข้าใจง่าย
4. **File Naming:** ใช้ชื่อไฟล์ตามหัวข้อโน๊ต
5. **Responsive:** ตรวจสอบว่า PDF ดูดีในขนาดต่างๆ

---

🎉 **เสร็จสมบูรณ์!** ตอนนี้คุณสามารถ export โน๊ตเป็น PDF ได้แล้ว!
