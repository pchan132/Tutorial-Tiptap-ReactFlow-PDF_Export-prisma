# ระบบทำเอกสารออนไลน์ด้วย Tiptap

เอกสารนี้อธิบายวิธีการใช้งานระบบทำเอกสารออนไลน์ที่สร้างด้วย Next.js, Tiptap และ PostgreSQL

## สารบัญ

1. [ภาพรวมของระบบ](#ภาพรวมของระบบ)
2. [โครงสร้างโปรเจค](#โครงสร้างโปรเจค)
3. [การติดตั้งและการตั้งค่า](#การติดตั้งและการตั้งค่า)
4. [ฐานข้อมูล](#ฐานข้อมูล)
5. [API Routes](#api-routes)
6. [Components](#components)
7. [การใช้งานระบบ](#การใช้งานระบบ)
8. [การ Export เป็น PDF](#การ-export-เป็น-pdf)
9. [การปรับแต่งและพัฒนาต่อ](#การปรับแต่งและพัฒนาต่อ)

## ภาพรวมของระบบ

ระบบทำเอกสารออนไลน์นี้มีคุณสมบัติหลักๆ ดังนี้:

- ✏️ **Editor ขั้นสูง**: ใช้ Tiptap ซึ่งเป็น rich text editor ที่ทันสมัย
- 📄 **การจัดการหลายหน้า**: สามารถสร้างเอกสารที่มีหลายหน้าได้
- 💾 **บันทึกลงฐานข้อมูล**: ใช้ PostgreSQL สำหรับเก็บข้อมูลเอกสาร
- 📥 **Export PDF**: สามารถส่งออกเอกสารเป็นไฟล์ PDF ได้
- 🎨 **UI ที่ใช้งานง่าย**: สร้างด้วย Tailwind CSS และ Lucide React Icons
- 🔄 **Auto-save**: บันทึกข้อมูลอัตโนมัติทุก 3 วินาที

## โครงสร้างโปรเจค

```
tiptap-ReactFlow-PDF_tutorial/
├── app/
│   ├── api/
│   │   ├── documents/           # API routes สำหรับเอกสาร
│   │   │   ├── route.ts         # GET และ POST สำหรับ documents
│   │   │   └── [id]/
│   │   │       └── route.ts     # GET, PUT, DELETE สำหรับ document ตาม ID
│   │   └── notes/               # API routes สำหรับ notes (มีอยู่แล้ว)
│   ├── documents/
│   │   ├── page.tsx             # หน้าแสดงรายการเอกสาร
│   │   ├── globals.css          # CSS สำหรับ document editor
│   │   └── [id]/
│   │       └── edit/
│   │           └── page.tsx     # หน้าสร้าง/แก้ไขเอกสาร
│   └── ...
├── components/
│   ├── DocumentEditor.tsx        # Component หลักสำหรับแก้ไขเอกสาร
│   ├── TiptapEditor.tsx         # Component พื้นฐาน (มีอยู่แล้ว)
│   └── ui/                      # UI components ต่างๆ
├── lib/
│   ├── pdfExport.ts             # Utility สำหรับ export PDF
│   ├── prisma.ts                # การเชื่อมต่อ Prisma
│   └── utils.ts                 # Utility functions
├── prisma/
│   ├── schema.prisma            # โครงสร้างฐานข้อมูล
│   └── migrations/              # Migration files
└── ...
```

## การติดตั้งและการตั้งค่า

### 1. การติดตั้ง Dependencies

โปรเจคนี้ใช้ dependencies หลักๆ ดังนี้:

```json
{
  "dependencies": {
    "@tiptap/react": "^3.10.0",
    "@tiptap/starter-kit": "^3.10.0",
    "@tiptap/extension-underline": "^3.10.1",
    "@tiptap/extension-text-align": "^3.10.1",
    "@tiptap/extension-color": "^3.10.1",
    "@tiptap/extension-font-family": "^3.10.1",
    "@tiptap/extension-image": "^3.10.1",
    "@tiptap/extension-link": "^3.10.1",
    "@tiptap/extension-table": "^3.10.1",
    "@tiptap/extension-placeholder": "^3.10.1",
    "@tiptap/extension-character-count": "^3.10.1",
    "jspdf": "^3.0.3",
    "html2canvas": "^1.4.1",
    "next": "16.0.1",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "@prisma/client": "^6.18.0",
    "prisma": "^6.18.0",
    "tailwindcss": "^4",
    "lucide-react": "^0.548.0"
  }
}
```

### 2. การตั้งค่าฐานข้อมูล

1. สร้างไฟล์ `.env.local` และเพิ่มค่าต่อไปนี้:

```env
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"
DIRECT_URL="postgresql://username:password@localhost:5432/database_name"
```

2. รัน Prisma migrations:

```bash
npx prisma migrate dev
```

3. สร้าง Prisma client:

```bash
npx prisma generate
```

### 3. การรันโปรเจค

```bash
npm run dev
```

เปิด http://localhost:3000 ใน browser เพื่อเข้าใช้งาน

## ฐานข้อมูล

### Document Model

```prisma
model Document {
  id          String   @id @default(cuid())
  title       String
  content     Json
  pageCount   Int      @default(1)
  metadata    Json?    // เก็บข้อมูลเพิ่มเติม เช่น font, margin, etc.
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

### การจัดเก็บข้อมูล

- **content**: เก็บข้อมูลเป็น JSON format จาก Tiptap editor
- **pages**: ในระบบจัดการเป็น array ของ pages แต่ในฐานข้อมูลเก็บเฉพาะ content ของ page แรก
- **metadata**: เก็บข้อมูลเพิ่มเติม เช่น font settings, margins, etc.

## API Routes

### GET /api/documents

ดึงรายการเอกสารทั้งหมด

**Response:**
```json
[
  {
    "id": "document_id",
    "title": "Document Title",
    "pageCount": 3,
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-01-01T00:00:00.000Z"
  }
]
```

### POST /api/documents

สร้างเอกสารใหม่

**Request Body:**
```json
{
  "title": "New Document",
  "content": { "type": "doc", "content": [] },
  "pageCount": 1,
  "metadata": {}
}
```

### GET /api/documents/[id]

ดึงข้อมูลเอกสารตาม ID

**Response:**
```json
{
  "id": "document_id",
  "title": "Document Title",
  "content": { "type": "doc", "content": [...] },
  "pageCount": 3,
  "metadata": {},
  "createdAt": "2025-01-01T00:00:00.000Z",
  "updatedAt": "2025-01-01T00:00:00.000Z"
}
```

### PUT /api/documents/[id]

อัปเดตเอกสาร

**Request Body:** เหมือนกับ POST

### DELETE /api/documents/[id]

ลบเอกสาร

## Components

### DocumentEditor Component

Component หลักสำหรับแก้ไขเอกสาร มีฟีเจอร์:

- **Toolbar**: เครื่องมือสำหรับจัดรูปแบบข้อความ
- **Page Navigation**: การสลับหน้าและจัดการหน้า
- **Auto-save**: บันทึกอัตโนมัติ
- **Character Count**: แสดงจำนวนตัวอักษรและคำ

#### Props

```typescript
interface Props {
  document?: Document;
  onChange: (document: Document) => void;
  onExportPDF?: () => void;
}
```

#### Tiptap Extensions

- `StarterKit`: Extensions พื้นฐาน
- `Underline`: ขีดเส้นใต้
- `TextAlign`: จัดข้อความซ้าย/กลาง/ขวา
- `Color`: สีข้อความ
- `FontFamily`: ฟอนต์
- `Image`: รูปภาพ
- `Link`: ลิงก์
- `Table`: ตาราง
- `Placeholder`: ข้อความแนะนำ
- `CharacterCount`: นับตัวอักษร

## การใช้งานระบบ

### 1. การสร้างเอกสารใหม่

1. ไปที่หน้า `/documents`
2. คลิกปุ่ม "New Document"
3. กรอกชื่อเอกสาร
4. เริ่มเขียนเนื้อหา
5. ระบบจะบันทึกอัตโนมัติทุก 3 วินาที

### 2. การแก้ไขเอกสาร

1. จากหน้ารายการเอกสาร คลิก "Open Document" หรือไอคอนแก้ไข
2. แก้ไขเนื้อหาตามต้องการ
3. ใช้ toolbar เพื่อจัดรูปแบบข้อความ
4. บันทึกด้วยปุ่ม Save หรือรอ auto-save

### 3. การจัดการหน้า

- **เพิ่มหน้าใหม่**: คลิกปุ่ม `+` ในส่วน Page Navigation
- **สลับหน้า**: คลิกหมายเลขหน้า
- **ลบหน้า**: คลิกปุ่มถังขยะ (ถ้ามีมากกว่า 1 หน้า)

### 4. Toolbar Features

#### Text Formatting
- **Bold** (B): ตัวหนา
- **Italic** (I): ตัวเอียง
- **Underline** (U): ขีดเส้นใต้

#### Headings
- **H1**: หัวข้อระดับ 1
- **H2**: หัวข้อระดับ 2
- **H3**: หัวข้อระดับ 3

#### Text Alignment
- **Align Left**: จัดชิดซ้าย
- **Align Center**: จัดกึ่งกลาง
- **Align Right**: จัดชิดขวา
- **Align Justify**: เต็มแถว

#### Lists
- **Bullet List**: รายการแบบจุด
- **Ordered List**: รายการแบบตัวเลข

#### Other Features
- **Quote**: ข้อความคำพูด
- **Link**: เพิ่มลิงก์
- **Image**: เพิ่มรูปภาพ
- **Table**: เพิ่มตาราง

#### Table Controls (เมื่อ cursor อยู่ในตาราง)
- **+Col/-Col**: เพิ่ม/ลบคอลัมน์
- **+Row/-Row**: เพิ่ม/ลบแถว
- **Delete Table**: ลบตาราง
- **HCol/HRow/HCell**: สลับสถานะ header

## การ Export เป็น PDF

ระบบรองรับการ export เอกสารเป็น PDF ได้ 2 วิธี:

### 1. HTML to Canvas Method (ค่าเริ่มต้น)

- ใช้ `html2canvas` แปลง HTML เป็นรูปภาพ
- ใช้ `jsPDF` สร้างไฟล์ PDF
- รักษาการจัดรูปแบบได้ดีกว่า

### 2. Text-based Method (ทางเลือก)

- แปลง Tiptap JSON เป็นข้อความธรรมดา
- ใช้ `jsPDF` วาดข้อความโดยตรง
- ขนาดไฟล์เล็กกว่าแต่รักษารูปแบบได้น้อยกว่า

### การ Export

1. คลิกปุ่ม "Export PDF" ใน toolbar
2. รอระบบประมวลผล
3. ไฟล์ PDF จะถูกดาวน์โหลดโดยอัตโนมัติ

## การปรับแต่งและพัฒนาต่อ

### 1. เพิ่ม Extensions ใหม่

เพิ่ม Tiptap extensions ใน `DocumentEditor.tsx`:

```typescript
import Highlight from "@tiptap/extension-highlight";

// ใน useEditor
extensions: [
  // ... existing extensions
  Highlight,
],
```

### 2. ปรับแต่ง PDF Export

แก้ไข `lib/pdfExport.ts` เพื่อปรับแต่ง:

- ขนาดกระดาษ
- Margin
- Font settings
- Header/Footer
- Watermark

### 3. เพิ่ม Authentication

เพิ่มการยืนยันตัวตน:

```typescript
// ใน API routes
import { getServerSession } from "next-auth";
import { authOptions } from "@/lib/auth";

export async function POST(req: Request) {
  const session = await getServerSession(authOptions);
  if (!session) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }
  // ... rest of the code
}
```

### 4. เพิ่ม Collaboration Features

ใช้ `@tiptap/extension-collaboration` และ `@tiptap/extension-collaboration-cursor`:

```typescript
import { Collaboration } from "@tiptap/extension-collaboration";
import { CollaborationCursor } from "@tiptap/extension-collaboration-cursor";
import { HocuspocusProvider } from "@hocuspocus/provider";

const provider = new HocuspocusProvider({
  appId: "your-app-id",
  url: "ws://localhost:1234",
  name: document.id,
});

// ใน useEditor
extensions: [
  // ... existing extensions
  Collaboration.configure({
    document: provider.document,
  }),
  CollaborationCursor.configure({
    provider: provider,
    user: {
      name: "User Name",
      color: "#f783ac",
    },
  }),
],
```

### 5. เพิ่ม Template System

สร้าง template สำหรับเอกสาร:

```typescript
interface Template {
  id: string;
  name: string;
  content: any;
  thumbnail?: string;
}

const templates: Template[] = [
  {
    id: "letter",
    name: "Business Letter",
    content: { /* Tiptap JSON */ },
  },
  // ... more templates
];
```

### 6. เพิ่ม Version History

ติดตามการเปลี่ยนแปลงของเอกสาร:

```prisma
model DocumentVersion {
  id        String   @id @default(cuid())
  documentId String
  content   Json
  createdAt DateTime @default(now())
  
  document  Document @relation(fields: [documentId], references: [id])
}
```

### 7. เพิ่ม Search Functionality

ใช้全文 search หรือ external search service:

```typescript
// API route สำหรับค้นหา
export async function GET(req: Request) {
  const { q } = new URL(req.url).searchParams;
  
  const documents = await prisma.document.findMany({
    where: {
      OR: [
        { title: { contains: q, mode: 'insensitive' } },
        // ค้นหาใน content ต้องแปลง JSON เป็น string ก่อน
      ],
    },
  });
  
  return NextResponse.json(documents);
}
```

## ข้อสังเกตและข้อจำกัด

1. **Performance**: สำหรับเอกสารขนาดใหญ่ อาจมีปัญหา performance ควรพิจารณา:
   - Virtual scrolling
   - Lazy loading
   - Pagination ในระดับ content

2. **Browser Compatibility**: ฟีเจอร์บางอย่างอาจไม่ทำงานใน browser เก่า
   - ทดสอบใน browser หลักๆ (Chrome, Firefox, Safari, Edge)
   - พิจารณา polyfills สำหรับฟีเจอร์ใหม่ๆ

3. **Security**:
   - Sanitize HTML content ก่อนบันทึก
   - Validate file uploads สำหรับรูปภาพ
   - Implement rate limiting สำหรับ API

4. **Scalability**:
   - พิจารณา CDN สำหรับรูปภาพ
   - Database indexing สำหรับการค้นหา
   - Caching strategy สำหรับเอกสารที่เข้าถึงบ่อย

## สรุป

ระบบทำเอกสารออนไลน์นี้ใช้เทคโนโลยีที่ทันสมัยและมีประสิทธิภาพสูง ด้วย Tiptap editor ทำให้มีความยืดหยุ่นในการจัดรูปแบบข้อความ และสามารถพัฒนาต่อได้ง่าย ระบบนี้เหมาะสำหรับ:

- การทำเอกสารภายในองค์กร
- การเขียนบทความและรายงาน
- การสร้างเอกสาร template
- การทำงานร่วมกันแบบ real-time (ถ้าเพิ่ม collaboration features)

หวังว่าเอกสารนี้จะเป็นประโยชน์ในการศึกษาและพัฒนาระบบต่อไปครับ!